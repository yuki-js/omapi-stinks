<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.OmapiStinks">
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LogDetailActivity"
            android:exported="false"
            android:label="Log Detail"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Persistent BroadcastReceiver - captures logs even when app is closed -->
        <!-- exported=true is required to receive broadcasts from other processes (Xposed hooks) -->
        <receiver
            android:name=".core.LogReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.aoki.yuki.omapistinks.LOG_ENTRY" />
            </intent-filter>
        </receiver>

        <!-- Xposed Module Metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Monitor OMAPI (Open Mobile API) calls" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        
        <!-- LSPosed Scope - Suggest which packages to hook -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>

</manifest>
